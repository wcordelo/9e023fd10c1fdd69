"""
Test-only values. Do not put real credentials here.

`TEST_NEON_CODE` is a fake vessel code used by offline tests (handshake auth shape).
"""

# 16 hex chars, same shape as a real Neon code; not a secret.
TEST_NEON_CODE = "deadbeefcafebabe"
